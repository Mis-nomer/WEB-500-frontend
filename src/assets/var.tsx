export interface IDish {
    id: number;
    name: string;
    image: string;
    category: string;
    description: string;
    price: number;
  }

export const fields: string[] = ["name", "price", "description", ""];
